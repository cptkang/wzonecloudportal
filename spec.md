# 클라우드 포탈 요건 정의서

> **작성 상태**: 스켈레톤. `[TODO]` 항목은 사용자가 확정해야 합니다.
> requirements-analyst 에이전트는 이 파일을 입력으로 `docs/01_requirements.md`를 생성합니다.
> `[TODO]`가 남아 있는 항목은 요구사항 문서에서 "미확정"으로 표시하고, 임의로 채우지 마세요.

## 1. 프로젝트 개요

### 1.1 목적

VMware vCenter와 Microsoft Hyper-V 관리 콘솔을 연결하여, 각 하이퍼바이저에 생성된 가상자원을
**단일 포탈 화면에서 통합 조회·제어·프로비저닝**할 수 있는 클라우드 포탈을 구축한다.

### 1.2 핵심 워크플로우

```
하이퍼바이저 접속 프로필 등록 (vCenter / Hyper-V)
    ↓
인벤토리 수집 워커가 주기적으로 자원 정보 폴링
    ↓
하이퍼바이저 고유 모델 → 공통 도메인 모델로 정규화
    ↓
인벤토리 캐시(DB/Redis)에 저장
    ↓
포탈 사용자가 테넌트 스코프 내에서 자원 조회 (캐시 기반)
    ↓
자원 제어/프로비저닝 요청 → 권한 검사 → 비동기 Task 등록
    ↓
Task 실행기가 커넥터를 통해 하이퍼바이저에 명령 전달
    ↓
Task 상태 추적 + 감사 로그 기록 + 인벤토리 갱신
```

### 1.3 기술 스택

| 구분 | 기술 | 비고 |
|------|------|------|
| API 서버 | FastAPI + uvicorn | 웹 UI + REST API |
| vCenter 연동 | pyVmomi | vSphere Automation SDK 병용 검토 |
| Hyper-V 연동 | pypsrp / WinRM | PowerShell Remoting |
| DB | PostgreSQL + SQLAlchemy (async) | 인벤토리 캐시, 사용자, 감사 로그 |
| 캐시 / 작업 큐 | Redis | 조회 캐시, 비동기 Task 큐 |
| 설정 | pydantic-settings | 타입 안전 환경변수 관리 |
| 인증·인가 | JWT + RBAC | 테넌트 스코프 격리 |
| 테스트 | pytest + pytest-asyncio | 계약 테스트 포함 |

---

## 2. 관리 대상 자원

| 자원 | vCenter | Hyper-V | 비고 |
|------|---------|---------|------|
| VM (가상 머신) | VirtualMachine | VM | 공통 |
| Host (하이퍼바이저 호스트) | HostSystem | Hyper-V Host | 공통 |
| Cluster | ClusterComputeResource | Failover Cluster | 개념 대응 규칙 확정 필요 |
| Datastore (스토리지) | Datastore | CSV / SMB 공유 | 개념 대응 규칙 확정 필요 |
| Network | 포트그룹 (vSwitch/DVS) | 가상 스위치 | 개념 대응 규칙 확정 필요 |
| ResourcePool | ResourcePool | — | Hyper-V 미지원 처리 방식 확정 필요 |
| Snapshot | Snapshot | 체크포인트 | 공통 (명칭 상이) |
| Task | Task | Job | 공통 |

**정규화 매핑 규칙은 `docs/02_decision.md`에 결정으로 기록한다.**

---

## 3. 기능 요건

### 3.1 자원 조회 (Must-Have)

- 전체 자원 목록 조회 (하이퍼바이저 종류 무관 동일 포맷)
- 자원 상세 조회 (CPU/메모리/디스크/네트워크/전원 상태)
- 검색·필터링 (이름, 상태, 호스트, 클러스터, 태그)
- `[TODO]` 자원 사용률(성능 메트릭) 표시 범위 — 실시간 / 최근 N시간 / 미지원

### 3.2 자원 제어 (Must-Have)

- 전원 제어: 시작 / 정상 종료 / 강제 종료 / 재시작
- 스냅샷: 생성 / 목록 조회 / 삭제 / 복원
- `[TODO]` 리소스 변경(CPU·메모리·디스크 증설) 지원 여부 및 무중단 변경 요구 여부

### 3.3 프로비저닝 (Must-Have)

- 템플릿/클론 기반 VM 생성
- VM 삭제
- 네트워크·스토리지 할당
- `[TODO]` 신규 VM 생성 시 필수 입력 항목 및 명명 규칙
- `[TODO]` 승인 워크플로우 필요 여부 (요청 → 관리자 승인 → 실행)

### 3.4 인증·인가 (Must-Have)

- 사용자 로그인 (JWT)
- 역할 기반 권한 (조회 전용 / 운영자 / 관리자)
- 테넌트 스코프 격리
- `[TODO]` 외부 인증 연동 필요 여부 (AD / LDAP / SSO)

### 3.5 운영 관리 (Must-Have)

- 하이퍼바이저 접속 프로필 등록·수정·삭제 (자격증명 암호화 저장)
- 인벤토리 동기화 상태 조회 및 수동 동기화 실행
- 작업(Task) 이력 조회
- 감사 로그 조회

### 3.6 Nice-to-Have

- `[TODO]` 자원 사용률 대시보드 (차트)
- `[TODO]` 인벤토리 Excel 내보내기
- `[TODO]` 알림 (작업 실패, 자원 임계치 초과)
- `[TODO]` 비용/할당량(Quota) 관리

---

## 4. 비기능 요건

### 4.1 성능

- `[TODO]` 관리 대상 규모: 하이퍼바이저 수, 호스트 수, VM 수
- `[TODO]` 인벤토리 동기화 주기 및 허용 지연 시간
- 목록 조회 응답: 캐시 기반 1초 이내 목표
- 하이퍼바이저 호출 타임아웃: 기본 60초, 재시도 최대 3회

### 4.2 보안

- 하이퍼바이저 자격증명 암호화 저장, 로그·응답·예외에 노출 금지
- 파괴적 작업(삭제, 강제 종료, 스냅샷 복원, 디스크 분리)은 명시적 확인 + 감사 로그 필수
- 모든 조회·변경은 테넌트 스코프로 필터링
- 모든 상태 변경 작업의 감사 로그 기록 (누가/언제/무엇을/어디서/결과)
- `[TODO]` 인증서 검증 정책 (자체 서명 인증서 허용 여부)
- `[TODO]` 서비스 계정 최소 권한 요구사항

### 4.3 안정성

- 쓰기 작업은 Task ID 기반 멱등 처리
- 하이퍼바이저 일부 장애 시 나머지 하이퍼바이저 자원은 정상 조회 가능 (부분 실패 허용)
- 동기화 실패 시 마지막 성공 시각과 함께 캐시 데이터 제공

### 4.4 확장성

- 새 하이퍼바이저 종류(예: Proxmox, Nutanix) 추가 시 어댑터 구현만으로 지원 가능
- `[TODO]` 향후 퍼블릭 클라우드(AWS/Azure) 연동 계획 유무

---

## 5. 제약 조건

- 운영 하이퍼바이저를 대상으로 개발·테스트 중 파괴적 작업을 실행하지 않는다.
- 자동 테스트는 목(mock) 커넥터를 사용한다.
- `[TODO]` 네트워크 제약 (포탈 서버에서 vCenter/Hyper-V 호스트로의 방화벽 허용 범위)
- `[TODO]` 배포 환경 (온프레미스 서버 / 컨테이너 / OS)

---

## 6. Phase 계획

| Phase | 범위 |
|-------|------|
| Phase 1 | 도메인 모델, 커넥터 Protocol, vCenter/Hyper-V 어댑터, 인벤토리 동기화 + 조회 API |
| Phase 2 | 전원 제어·스냅샷, 비동기 Task 관리, 감사 로그 |
| Phase 3 | 프로비저닝(생성/삭제), 인증·RBAC·테넌트 격리 |
| Phase 4 | 포탈 UI, 대시보드, 운영 관리 화면 |

`[TODO]` Phase 우선순위와 일정 확정
